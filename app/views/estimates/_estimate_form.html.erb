<%= simple_form_for [@estimate] do |f| %>
    <%= f.input :date, as: :date %>
    <%= f.input :address, as: :string, input_html: {data: { autocomplete_source: Address.autocomplete_source }} %>
    <%= f.hidden_field :address_id %>

    <%= f.button :submit %>
<% end %>
